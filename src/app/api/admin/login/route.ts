import {NextResponse} from 'next/server';import {adminCookie,expectedToken,passwordMatches} from '../../../../lib/admin-auth';
export async function POST(request:Request){const body=await request.json().catch(()=>({}));const token=expectedToken();if(!token)return NextResponse.json({error:'Administration is not configured.'},{status:503});if(!passwordMatches(String(body.password||'')))return NextResponse.json({error:'Invalid password.'},{status:401});const response=NextResponse.json({ok:true});response.cookies.set(adminCookie,token,{httpOnly:true,sameSite:'strict',secure:process.env.NODE_ENV==='production',path:'/',maxAge:60*60*8});return response;}
