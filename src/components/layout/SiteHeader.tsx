'use client';
import Link from 'next/link';
import Image from 'next/image';
import {useEffect,useState} from 'react';
const links=[['About','/about'],['Expertise','/#expertise'],['Projects','/projects'],['Founder','/about#founder'],['Contact','/consultation']];
export default function SiteHeader(){const [open,setOpen]=useState(false);const [scrolled,setScrolled]=useState(false);useEffect(()=>{const update=()=>setScrolled(window.scrollY>24);update();addEventListener('scroll',update,{passive:true});return()=>removeEventListener('scroll',update)},[]);useEffect(()=>{document.body.style.overflow=open?'hidden':'';return()=>{document.body.style.overflow=''}},[open]);return <header className={`site-header ${scrolled?'scrolled':''} ${open?'menu-open':''}`}><Link className="brand-link" href="/" onClick={()=>setOpen(false)}><Image className="brand-logo" src="/logos/svj-mark-enhanced.png" alt="SVJ Engineers and Consultants" width={1536} height={1024} priority/></Link><nav className="nav desktop-nav" aria-label="Primary navigation">{links.map(([label,href])=><Link key={label} href={href as never}>{label}</Link>)}</nav><Link className="header-cta" href="/consultation">Book consultation</Link><button className="menu-toggle" type="button" aria-expanded={open} aria-controls="mobile-menu" onClick={()=>setOpen(v=>!v)}><span>{open?'Close':'Menu'}</span><i/><i/></button><nav id="mobile-menu" className="mobile-nav" aria-label="Mobile navigation">{links.map(([label,href],i)=><Link key={label} href={href as never} onClick={()=>setOpen(false)}><span className="mono">0{i+1}</span>{label}</Link>)}<Link className="button" href="/consultation" onClick={()=>setOpen(false)}>Book consultation ↗</Link></nav></header>}
